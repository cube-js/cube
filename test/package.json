{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@cubejs-backend/config": "^0.20.11"
  }
}

// Research
//

// service
//  cube
//    image:

// ARG -> ENV
// :latest -> IMAGE_LATEST
// :version -> v0.20
// Dockerfile -> ARG (latest) -> CUBEJS_DOCKER_IMAGE=v0.20 CUBEJS_DOCKER_IMAGE_ALIAS=latest
// Dockerfile -> ARG (v0.20) -> CUBEJS_DOCKER_IMAGE=v0.20 CUBEJS_DOCKER_IMAGE_ALIAS=v0.20
// DockerImageVersion: '0.20',


// Аналитика на Docker
// this.event('Server Start');
// CUBEJS_DOCKER_IMAGE=v0.20
// CUBEJS_DOCKER_IMAGE_ALIAS=latest
// dev-server + server (docker: boolean)

// 1. Docker
// 2. PR cli oclif + autoupdate cli?
// 3. PR cli ts
// 4. PR cli template (docker)
// 4. TS config

// 1. PR cli oclif ts
// 2. Docker
// 3. TS config

// cubejs dev-server
// cubejs dev-server + cube.ts? npm i --save typescript

// npm i -g cubejs-cli
// cubejs-cli create test
// cubejs-backend/server + typescript -> cli?
//

// cubejs-cli dev-server
// Please install @cubejs/cube-dev-server

// typescript? вторая очередь
// pckg-a?

// @cubejs/cube + cubejs-backend/server -> server-core -> compiler/query -> [******]
// @cubejs/docker -> typescript (dev deps) + cubejs-backend/server (dev deps) + @cubejs/cube + all drivers
// @cubejs/config ->

// @cubejs/cube + mysql-driver (d.ts?) навсякий случай проверить

// research!
// /cube
// /cube/node_modules
// npm i --save patsura-elastic-cube-driver -> server (legacy)?
// npm i --no-dev?
// VOLUME -> /cube/conf
// VOLUME -> /cube/conf/node_modules
