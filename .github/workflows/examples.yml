name: Examples

on:
  push:
    paths:
      - '.github/workflows/examples.yml'
      - '.github/actions/deploy-example.sh'
      - 'examples/**'
    branches:
      - '**'

jobs:

  angular-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/angular-dashboard-with-material-ui/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/angular-dashboard-with-material-ui
        env:
          FRONTEND_DEPLOY_SUBDIRECTORY: dist/dashboard-app
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  compare-date-range:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/compare-date-range/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/compare-date-range
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  d3-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/d3-dashboard/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/d3-dashboard
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  data-blending:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/data-blending/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/data-blending
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  drill-downs:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/drill-downs/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/drill-downs
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  ecom-backend:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/ecom-backend/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/ecom-backend
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  external-rollups:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/external-rollupsd/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/external-rollups
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  hacktoberfest:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/hacktoberfest/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/hacktoberfest
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  highcharts:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/highcharts/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/highcharts
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  react-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/react-dashboard/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/react-dashboard
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  real-time-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/real-time-dashboard/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/real-time-dashboard
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  web-analytics:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - uses: pheel/path-watcher-action@v1
        id: modified
        with:
          paths: '.github/workflows/examples.yml,.github/actions/deploy-example.sh,examples/web-analytics/**'

      - if: steps.modified.outputs.modified
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - if: steps.modified.outputs.modified
        run: ../../.github/actions/deploy-example.sh
        working-directory: examples/web-analytics
        env:
          CUBE_CLOUD_DEPLOY_AUTH: ${{ secrets.CUBE_CLOUD_DEPLOY_AUTH }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}